type Query {
    hello: String
    getCharacters: [ Character ]
}

# custom types
# API de Legend of zelda
type Character {
    id: ID
    name: String
    race: Race
}

enum Race {
    HYLIAN
    GERUDO
    FAIRY
}

